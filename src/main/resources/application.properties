# Server
server.port=8080
spring.application.name=helloworld

# Anthropic SDK configuration (api-key is loaded from secrets.json)
anthropic.model=claude-sonnet-4-6
anthropic.max-tokens=1024

# Logging
logging.level.com.example=INFO